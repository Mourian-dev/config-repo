server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/orderflow_payments
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  kafka:
    producer:
      properties:
        spring.json.type.mapping: "paymentRefundedEvent:com.orderflow.payment.event.PaymentRefundedEvent"
    consumer:
      properties:
        spring.json.type.mapping: "orderCancelledEvent:com.orderflow.payment.event.OrderCancelledEvent"